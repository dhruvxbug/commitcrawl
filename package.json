{
  "name": "commitcrawl",
  "version": "1.0.0",
  "description": "A dungeon that only exists because people keep sending pull requests.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node engine/node/play.js",
    "validate": "node scripts/validate.js",
    "build:web": "node scripts/build-web-data.js"
  },
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
